/gsd:map-codebase --query
⎿  Read .claude\get-shit-done\workflows\map-codebase.md (unchanged)

● To proceed with the task of analyzing the existing codebase using parallel gsd-codebase-mapper agents, we will use the command /gsd:map-codebase
--query. This command will orchestrate the execution of multiple mapper agents to produce structured documents about the codebase. Each agent will
focus on a specific area and write its document directly to the .planning/codebase/ directory.

Here's how we can proceed with this task:

1. Check if .planning/STATE.md exists: This file may contain context that is relevant for understanding the codebase state, such as previous scans or  analyses. If it exists, load and use this context to inform our analysis.
2. Create .planning/codebase/ directory structure: Ensure that there is a suitable directory structure in place where the documents generated by the
   agents will be stored.
3. Spawn 4 parallel gsd-codebase-mapper agents: Each agent will focus on a different aspect of the codebase:
   - Agent 1: Focus on technology (tech) and will write STACK.md and INTEGRATIONS.md.
   - Agent 2: Focus on architecture (arch) and will write ARCHITECTURE.md and STRUCTURE.md.
   - Agent 3: Focus on quality (quality) and will write CONVENTIONS.md and TESTING.md.
   - Agent 4: Focus on concerns (concerns) and will write CONCERNS.md.
4. Wait for agents to complete: The script should monitor the progress of each agent and ensure that all agents have completed their tasks before
   proceeding. This can be done by waiting for confirmations from the agents (not the contents of the documents).
5. Verify all 7 documents exist with line counts: After the agents have completed, check to make sure that all seven documents (STACK.md,
   INTEGRATIONS.md, ARCHITECTURE.md, STRUCTURE.md, CONVENTIONS.md, TESTING.md, and CONCERNS.md) exist in the .planning/codebase/ directory, and verify
   that they contain a reasonable number of lines.
6. Commit codebase map: Once all documents are verified to be present and correctly formatted, commit these documents as part of the codebase map.
7. Offer next steps: After completing the analysis, inform the user about what comes next. Typically, this would involve either starting a new
   project with /gsd-new-project or planning the next phase of development after updating the codebase understanding.